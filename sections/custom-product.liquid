<h1>{{ product.title }}</h1>
<p>{{ product.description }}</p>

{% schema %}
  {
    "name": "Custom Product",
    "settings": [],
    "presets": [
      {
        "name": "Custom Product"
      }
    ]
  }
{% endschema %}